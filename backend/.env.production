# Production Environment Configuration for Render
# Este archivo se usa cuando no existe .env (en producción)

# Server Configuration
HOST=0.0.0.0
PORT=10000

# URL Configuration for unified routing
PUBLIC_URL=https://tudominio.com/scouteando
ADMIN_PATH=/scouteando/admin
STRAPI_ADMIN_BACKEND_URL=https://tudominio.com/scouteando

# Security Keys - Configura estos valores en Render Dashboard
APP_KEYS=${APP_KEYS}
API_TOKEN_SALT=${API_TOKEN_SALT}
ADMIN_JWT_SECRET=${ADMIN_JWT_SECRET}
TRANSFER_TOKEN_SALT=${TRANSFER_TOKEN_SALT}
JWT_SECRET=${JWT_SECRET}
ENCRYPTION_KEY=${ENCRYPTION_KEY}

# Database Configuration for PostgreSQL
DATABASE_CLIENT=postgres
DATABASE_URL=${DATABASE_URL}
DATABASE_SSL=true

# Production Settings
NODE_ENV=production
